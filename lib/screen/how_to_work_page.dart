import 'package:flutter/material.dart';

class HowToWorkPage extends StatelessWidget {
  const HowToWorkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Center(
        child: Text('This page explains how to work with the app.'),
      
    );
  }
}